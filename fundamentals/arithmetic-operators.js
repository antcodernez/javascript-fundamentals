// integers & decimal

const integer = 32;
const decimal = 2.5;

// both numbers are type number

// scientific notation
const scientific = 5e4; // means 5 * 10^5 or 50,000

// infinite & NaN
const infinite = Infinity;
const NotNum = NaN;


// Arithmetic operations

// Addition, subtraction, multiplication and division

const addition = 4 + 5;
const subtraction = 4 - addition;
const multiplication = 4 * 5;
const division = 4 / 5;

// module & exponentiation

const moduleOperation = addition % 2;
const potenci = addition ** 2;

// precision


const result = 0.2 + 0.1;

console.log(result);
console.log(result.toFixed(1)); // how many numbers I want to show after the point


// advance operatations

const squeareFrom4 = Math.sqrt(16);
const absoluteValue = Math.abs(43); // If I want an absolute value 
const random = Math.random();

console.log(squeareFrom4);
console.log(absoluteValue);
console.log(random);

