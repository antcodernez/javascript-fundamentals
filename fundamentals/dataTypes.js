// primitive

// strings
let name = "Ana Yareli";

// number
let ageAna = 28;

// boolean
let hasMayorityAge = true;

// null
let userdata = null;

// undefined; this type of data are the default javascript let this 
let catdata = undefined;

// symbol; indicates a unique value 
let catId = Symbol("Mishifu");

// BigInt
let number = 2n;

// Complexes

// object

const cats = {}
cats.genus = "felinidae";

// arrays
const fruits = ["apple", "orange", "watermelon", "banana"];


// functions

function sayHello (name) {
    console.log(`Hi minino ${name}`);
}

const hiCat = (name) => {
    console.log(`Hi minino ${name}`);
}